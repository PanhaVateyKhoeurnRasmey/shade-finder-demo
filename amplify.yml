version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd shade-finder-demo
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: /shade-finder-demo/build
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
